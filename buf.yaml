version: v2

modules:
  - path: proto

lint:
  use:
    - STANDARD
  except:
    # 양방향 스트리밍 oneof 래퍼 패턴은 RPC 네이밍 규칙 예외 처리
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
    - RPC_REQUEST_RESPONSE_UNIQUE

breaking:
  use:
    - FILE
